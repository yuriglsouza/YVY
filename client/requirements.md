## Packages
recharts | For visualizing satellite indices trends
react-leaflet | For displaying farm maps
leaflet | Core map library
date-fns | For date formatting in reports and charts
framer-motion | For smooth page transitions and UI animations

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Leaflet requires CSS import in index.css
